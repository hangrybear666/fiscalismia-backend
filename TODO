# Todo List for [Fiscalismia Backend]

### High Prio
- Limit access to Admin Panel Menu to jwt-session token content via backend check
- fix XSS in TSV input send commands by using e.g. pg-format

### Medium Prio
- Evaluate if ssl can be used for demo development database setting ssl = true
- Rethink localbrowser storage evaluation, especially in regards to loginUserName, to prohibit forgery

### Low Prio
- Switch to assymettric key encryption JWT encoding not static password due to forgery on jwt secret leak
- Add POST Route for Variable Expenses Staging
- Add Supertest for evaluation of this post route and the ETL process
- consider moving AWS_API_GATEWAY_ENDPOINT to env var to not leak it publicly

### Post DEMO Deployment
- check the `app.set('trust proxy', 1) setting` in production by hitting the api/ip endpoint to find out of the ip address of the X-Forwarded-For header matches the actual ip address or that of a reverse proxy or load balancer
- Migrate away from dotenv. Dotenv maintainer is being an asshole - use --env-file=.env for node and --node-args='--env-file=.env' for nodemon and NODE_OPTIONS='--env-file=.env'
- Rethink public folder storage for production env -> Persistence remote with backup / HA